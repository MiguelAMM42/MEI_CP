
#include "../include/utils.h"

int main() {
   
    initialize();
    //testIniatialize();  
    // começar em K pq o K-1 já estão no cluster
    //geometricCenter();
    attribution(K);
    kmeans();
    return 0;
}